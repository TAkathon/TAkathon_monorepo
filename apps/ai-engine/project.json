{
    "name": "ai-engine",
    "projectType": "application",
    "sourceRoot": "apps/ai-engine",
    "targets": {
        "dev": {
            "executor": "nx:run-commands",
            "options": {
                "command": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8001",
                "cwd": "apps/ai-engine"
            }
        }
    }
}